{
  "name": "pp-nextjs-json-guard",
  "private": true,
  "scripts": {
    "bad": "node scripts/generate.js bad > fixtures/current-output.jsonl && cat fixtures/current-output.jsonl",
    "good": "node scripts/generate.js good > fixtures/current-output.jsonl && cat fixtures/current-output.jsonl", 
    "check": "node ../../packages/cli/dist/index.js eval -c .promptproof/rules.yml",
    "try": "pnpm bad && pnpm check || true",
    "fix": "pnpm good && pnpm check"
  },
  "devDependencies": {}
}
